query Feed($first: Int) {
    feeds(first: $first) {
        edges {
            node {
                id
                post
                user {
                    username
                    profile {
                        pictureUrl
                    }
                }
                likes
                comments
            }
        }
    }
}
