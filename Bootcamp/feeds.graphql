query Feed($first: Int) {
    feeds(first: $first) {
        edges {
            node {
                id
                date
                post
                user {
                    username
                    profile {
                        pictureUrl
                    }
                }
                likes
                comments
            }
        }
    }
}
